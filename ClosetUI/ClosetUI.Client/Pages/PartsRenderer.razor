@page "/PartsRenderer"
@* @attribute [StreamRendering(true)] *@
@rendermode InteractiveWebAssembly

<PageTitle>Parts Drawer</PageTitle>

<HeadContent>
    <meta name="description" content="Carpentry Parts Generator, Parts Drawer"
</HeadContent>

<h1 class="text-center">Parts Drawer</h1>

@if (ParamsResult == null && ErrorMessage == null)
{
    <DisplaySpinner />
}
else if (!string.IsNullOrWhiteSpace(ErrorMessage))
{
    <DisplayError ErrorMessage="@ErrorMessage" />
}
else
{
    @* <svg width="200" height="200" id="@CanvasId">
        <label>Parts Generator</label>
        @foreach (var part in ParamsResult.Parts)
        {
            <rect x="@part.X" y="@part.Y" width="@part.Wt" height="@part.Ht" style="fill:blue;stroke:black;stroke-width:2;fill-opacity:0.1;stroke-opacity:0.9" />
        }
    </svg> *@
    <canvas width="100%" height="100%" id="@CanvasId"></canvas>
}

